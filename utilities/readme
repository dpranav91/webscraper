Download chromedrivers from below url for centos:

$ wget https://chromedriver.storage.googleapis.com/2.40/chromedriver_linux64.zip --no-check-certificate
$ unzip chromedriver_linux64.zip
$ chromedriver --version
